package com.ystartor.mapping;

import java.util.List;

/**
 *  在处理了任何动态内容之后，从{@link SqlSource}获得的实际SQL字符串。
 *  <p>
 *  SQL可能有SQL占位符“?”和参数映射列表(有序)，其中包含每个参数的附加信息(至少是要从中读取值的输入对象的属性名)。
 *  </p>
 *  还可以有由动态语言创建的附加参数(对于循环，bind…)。
 *
 */
public class BoundSql {

//    private final String sql;
//    private final List<>

}
